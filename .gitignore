# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# --- Virtual envs ---------------------------------------------------------
.venv/
venv/
env/

# --- Notebooks ------------------------------------------------------------
.ipynb_checkpoints/

# --- IDE / OS -------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Project artefacts ----------------------------------------------------
# Heavy CSV (> 100 MB) — regenerated by the pipeline
data/processed/clean_transactions.csv

# Aggregates + meta + KMeans models — regenerated by the pipeline
data/processed/agg_*.csv
data/processed/meta.json
models/kmeans.pkl
models/kmeans_scaler.pkl

# rfm_segmented.csv stays tracked (it's small, useful as a starter artefact)

# --- Streamlit ------------------------------------------------------------
.streamlit/secrets.toml

# --- Secrets / env --------------------------------------------------------
.env
.env.*
!.env.example

# --- Logs / temp ----------------------------------------------------------
*.log
*.tmp
